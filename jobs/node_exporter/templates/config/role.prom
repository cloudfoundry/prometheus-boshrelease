node_role{role="<%= spec.deployment %>/<%= spec.job.name %>/<%= spec.index %>", deployment="<%= spec.deployment %>", job_name="<%= spec.job.name %>", index="<%= spec.index %>", ip="<%= spec.address %>"} 1
