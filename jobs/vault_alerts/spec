---
name: vault_alerts

packages: []

templates:
  vault.alerts.yml: vault.alerts.yml

properties:
  vault_alerts.up.evaluation_time:
    description: "Vault exporter for '{{ $labels.instance }}' cannot talk to Vault"
    default: 5m
  vault_alerts.initialized.evaluation_time:
    description: "Vault '{{ $labels.instance }}' is uninitialized"
    default: 5m
  vault_alerts.sealed.evaluation_time:
    description: "Vault '{{ $labels.instance }}' is sealed"
    default: 5m
  vault_alerts.standby.evaluation_time:
    description: "There are {{ $value }} active Vault instance(s)"
    default: 5m
