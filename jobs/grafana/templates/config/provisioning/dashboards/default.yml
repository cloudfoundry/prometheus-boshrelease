# config file version
apiVersion: 1

# list of providers to insert/update depending
# whats available in the database
providers:
  - name: default
    type: file
    orgId: 1
    folder:
    editable: false
    disableDeletion: false
    options:
      path: /var/vcap/store/grafana/dashboards/default
