---
name: prometheus_dashboards

packages: []

templates:
  prometheus_scrapes_notifications.json: prometheus_scrapes_notifications.json
  prometheus_storage.json: prometheus_storage.json
  prometheus_targets.json: prometheus_targets.json

properties: {}
