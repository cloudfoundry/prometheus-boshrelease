#!/bin/bash

set -eux

# Extract consul_exporter package
mkdir -p ${BOSH_INSTALL_TARGET}/bin
tar xzvf ${BOSH_COMPILE_TARGET}/consul_exporter/consul_exporter-0.2.0.linux-amd64.tar.gz -C ${BOSH_INSTALL_TARGET}/bin
