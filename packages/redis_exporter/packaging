#!/bin/bash

set -eux

# Extract redis_exporter package
mkdir -p ${BOSH_INSTALL_TARGET}/bin
tar xzvf ${BOSH_COMPILE_TARGET}/redis_exporter/redis_exporter-v0.10.5.linux-amd64.tar.gz -C ${BOSH_INSTALL_TARGET}/bin
