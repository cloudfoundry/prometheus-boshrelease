#!/usr/bin/env bash

set -eux

# Copy common utils
mkdir -p ${BOSH_INSTALL_TARGET}/common
cp -a ${BOSH_COMPILE_TARGET}/common/* ${BOSH_INSTALL_TARGET}/common

# Extract logstash_exporter package
mkdir -p /tmp/logstash_exporter
tar -xzf ${BOSH_COMPILE_TARGET}/logstash_exporter/logstash_exporter-*.tar.gz -C /tmp/logstash_exporter

mkdir -p ${BOSH_INSTALL_TARGET}/bin
cp /tmp/logstash_exporter/logstash_exporter ${BOSH_INSTALL_TARGET}/bin/
chmod +x "${BOSH_INSTALL_TARGET}/bin/logstash_exporter"