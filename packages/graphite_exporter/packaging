#!/bin/bash

set -eux

# Extract graphite_exporter package
mkdir -p ${BOSH_INSTALL_TARGET}/bin
tar xzvf ${BOSH_COMPILE_TARGET}/graphite_exporter/graphite_exporter-0.1.0.linux-amd64.tar.gz -C ${BOSH_INSTALL_TARGET}/bin
